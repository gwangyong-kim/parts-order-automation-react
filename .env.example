# ==========================================
# PartSync MRP - Environment Variables
# ==========================================
# Copy this file to .env and fill in the values

# Database
# For SQLite (development):
DATABASE_URL="file:./dev.db"
# For PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@localhost:5432/partsync?schema=public"

# NextAuth.js
# Generate a secret: openssl rand -base64 32
AUTH_SECRET="your-secret-key-change-in-production"
AUTH_URL="http://localhost:3000"

# ==========================================
# Google OAuth (Optional - for Google login)
# ==========================================
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성
# https://console.cloud.google.com/apis/credentials
#
# 설정 방법:
# 1. Google Cloud Console에서 프로젝트 생성/선택
# 2. APIs & Services > Credentials > Create Credentials > OAuth client ID
# 3. Application type: Web application
# 4. Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
# 5. OAuth 동의 화면에서 테스트 사용자 추가 또는 앱 게시
#
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# App Configuration
NEXT_PUBLIC_APP_NAME="PartSync"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ==========================================
# Google Cloud Storage (Optional - for cloud backup)
# ==========================================
# GCS 버킷 이름 (필수)
GCS_BUCKET_NAME="your-bucket-name"

# GCS 프로젝트 ID
GCS_PROJECT_ID="your-gcp-project-id"

# 인증 방법 (택1):
# 방법 1: 서비스 계정 키 파일 경로
# GCS_KEY_FILE="/path/to/service-account-key.json"

# 방법 2: 서비스 계정 JSON을 환경 변수로 (Docker/Cloud Run용)
# GCS_CREDENTIALS='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'

# 백업 파일 저장 경로 접두사 (선택, 기본값: backups/)
# GCS_BACKUP_PREFIX="backups/"

# ==========================================
# Docker-specific (when using docker-compose)
# ==========================================
# These will be set automatically by docker-compose
# DATABASE_URL=file:/app/data/partsync.db
